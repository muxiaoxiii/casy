# Casy 未实现清单与实施记录（2026-08-19）

> **维护者**: Kimi Code（接手 Casy 开发）
> **核验方式**: 逐条对照源码 + `cargo check` + `npm run build`
> **背景**: 2026-08-19 凌晨 deepseek 批量新增了推荐/报表/学习/蒸馏/筛选/图谱/语音/MCP/SMTP/凭据等代码，
> 并在 `docs/devlog/TODO.md` 标记"全部已完成"。经源码核验与编译验证，**该清单过度承诺**：
> 10 项中没有一项完全接线，后端 54 个编译错误。本文档记录真实差距并逐条收口。

---

## 〇、总体验收门槛

- [x] `cargo check` 0 error（15 warning 均为存量 dead_code/unused，待后续清理）
- [x] `npm run build` 通过（exit 0）
- [x] `cargo test --lib` 68/68 通过（含 MCP server 5 项）
- [x] 每条"已实现"都能指出：后端命令 + invoke_handler 注册 + 前端调用入口（见下表）
- [x] `docs/devlog/TODO.md` 的"全部已完成"清单修正为真实状态

---

## 一、编译修复（P0，阻塞一切）

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| A1 | `run_blocking` 闭包内 `?` 无法把 `String` 转 `anyhow::Error`，5 个新命令全错 | `commands/mod.rs:313,333,345,357,369` | ☑ |
| A2 | MCP 模块：`CaseFilter` 字段名错误、`crate::tauri_call_safe` 未定义（11 处）、`get_tools`/`execute_tool`/`McpToolCall` 不存在却被 commands/mod.rs 引用、参数数量不匹配 | `mcp/mod.rs:457-489`, `commands/mod.rs:247-257` | ☑ |
| A3 | `query_map` 返回 `Result`，不能直接 `.collect()` | `ai/reports.rs:156,175` | ☑ |
| A4 | `pub mod smtp;` 插在 doc comment 中间（E0753） | `email/mod.rs:8-9` | ☑ |

## 二、功能接线缺口（P1 = 用户指定，P2 = 用户指定次优先）

### P1-1 AI 推荐引擎接通后端
- 后端 `ai/recommender.rs::generate_today_recommendations` 已实现规则排序，命令已注册
- ❌ 前端无人调用：`AICompanionView.vue:69-77` 是硬编码假数据（代码内注释自认），`HomeView.vue:429-456` 是本地排序
- 待办：AICompanionView「今日推荐」Tab 改调 `get_today_recommendations`；推荐项落地（写回 `today_index`/`estimated_minutes`）+ 采纳/拒绝记录进 `decisions`

### P1-2 自动报表后端（每日早报/每周总结）
- 后端只有 `generate_daily_brief`（每日），**无每周总结**；无定时调度
- ❌ HomeView「每日早报」横幅是静态文案，"08:00 自动生成"是写死的
- 待办：补 `generate_weekly_summary`；lib.rs 加定时调度（每日 08:00 早报 / 周日 21:00 周报，落 `daily_stats` + `smart_summaries`）；HomeView 早报横幅接 `generate_daily_brief_cmd`，AI 不可用时回退结构化版（设计哲学 §12.5 降级态）

### P1-3 Saved Filters（筛选规则可保存）
- ❌ `stores/filters.ts` 调用的 `list_saved_filters`/`save_filter`/`delete_filter` 后端完全不存在
- ❌ 无任何视图引用 `useFiltersStore`（孤儿代码）
- 待办：新建 `saved_filters` 表 + 3 个命令；接入 CaseListView / TasksView 筛选栏（保存/应用/删除）

### P2-1 知识图谱可视化
- ❌ `KnowledgeGraphView.vue` 直接用全局 `d3`，但**项目未依赖 d3**，打开即报错
- ❌ 知识↔案件的边是**随机连的**（代码注释自认"随机关联"）；KnowledgeView 无入口跳转
- 待办：按项目惯例（Dashboard 纯 SVG、不引图表库）重写为 SVG 力导向图；后端加 `get_knowledge_graph` 命令，用 `knowledge_relations`/`cases`/`tasks` 真实关联；KnowledgeView 加入口

### P2-2 行为学习闭环
- ⚠️ `task_events` 有写入点（created/completed/moved），分析函数存在
- ❌ `actual_minutes` 全后端**无写入点**（耗时校准永远空数据）
- ❌ 分析结果**从不写回** `estimated_minutes`；前端无调用
- 待办：任务完成时记录实际耗时（完成对话框可选填/按 start~done 估算）→ `task_events` + `tasks.actual_minutes`；学习分析写回 `estimated_minutes` 校准；AICompanionView 加「学习洞察」展示

### P2-3 数据蒸馏调度
- ⚠️ `run_distillation` 存在，命令已注册
- ❌ 无定时调度；候选记忆只在内存返回，**从不 INSERT 进 `memory_entries`**；查的 `deferred` 事件无写入点
- 待办：候选落库 `memory_entries`（L2，待确认状态）；任务延期时写 `deferred` 事件；lib.rs 周日 23:00 蒸馏调度（L1 90 天清理 + L2 提炼 + L3 归档）；确认区 UI（AICompanionView 或 ReminderView 旁）

### P2-4 语音速记
- ✅ 后端 `save_voice_note` + 前端 `useVoiceNote.ts` 完整
- ❌ `InboxView.vue` 未引入 `useVoiceNote`，无录音按钮
- 待办：InboxView 捕获条加语音按钮；转写为空时文案明确"语音已保存，待转写"

## 三、其他今晚新增代码的缺口

| # | 模块 | 现状 | 待办 |
|---|------|------|------|
| B1 | MCP Server | 8 工具定义存在但编译不过；lib.rs 从未启动监听 | 修编译；lib.rs setup 启动本地 HTTP JSON-RPC 端口（127.0.0.1，只读默认开放，写操作走确认） |
| B2 | SMTP 发送 | `send_raw_email` 是 TODO 空壳，连上 socket 什么都不发 | 手写极简 SMTP（EHLO/AUTH PLAIN/MAIL/RCPT/DATA，TLS 用 native-tls 若已有依赖）或明确降级为"生成 .eml 供用户发送" |
| B3 | IMAP 凭据 keychain | 迁移命令存在但编译错；email/mod.rs 仍走 base64 活路径；启动不自动迁移；前端无入口 | email/mod.rs 改走 `credentials::get_imap_password`（带 base64 回退）；启动时自动迁移；设置页加状态展示 |
| B4 | `docs/devlog/TODO.md` | 声称"所有 P0/P1/P2 已实现"，与实际不符 | 收口完成后更新为真实状态 |

## 四、设计哲学对照 — 已确认的更大缺口（记录在案，本轮不全部做）

- L2 蒸馏确认区、递归确认（11.5）、CalDAV 日历同步（M1）、动态字段系统（P4）、律师画像引导 — 属路线图后置项，不冒充已实现

---

## 实施记录

### 2026-08-19 — Kimi Code 收口实施

**文档整理**
- `docs/architecture.md` / `architecture-v3.md` / `architecture-plugin-system.md` / `casy-todo/` → `docs/archive/`；`docs/` 现仅保留 `casy-design-philosophy.md`（唯一总纲）+ `devlog/`
- 引用同步更新：`README.md`、`Casy-STATUS-v3.md`、设计哲学文档内 18 处 `architecture.md` 引用改指 `archive/architecture.md`

**一、编译修复（54 error → 0）**
- `commands/mod.rs`：5 个新命令 + `migrate_credentials_to_keychain` 的 `String→anyhow` 错误转换，改为 `.map_err(anyhow::Error::msg)` / 直接 `?`
- `mcp/mod.rs`：11 处 `crate::tauri_call_safe`（前端概念）改为直调真实后端函数；`CaseFilter`/`TaskFilter`/`KnowledgeFilter` 字段对齐真实定义；补齐 `get_tools`/`execute_tool`/`McpToolCall` 公开接口
- `ai/reports.rs`：`query_map` 后补 `?` 再 collect
- `email/mod.rs`、`ai/mod.rs`：`pub mod` 移出 doc comment 块
- `inbox.rs`：screenshots 0.6 API 修正；5 处 anyhow 转换

**二、功能接线（P1×3 + P2×4 + 其他×3 全部落地）**

| 项 | 落地内容 | 验证 |
|---|---------|------|
| P1-1 AI 推荐引擎 | 后端 `get_today_recommendations`（规则排序）；AICompanionView 今日推荐接真实数据 + 采纳/拒绝 → `record_decision`（confirmed/rejected）+ 采纳自动 `moveToToday`；首页智能推荐区同源 | cargo+build |
| P1-2 自动报表 | `generate_daily_brief` 落 `daily_stats`（对内）+ `smart_summaries`（对外，按日判重 UPSERT）；新增 `generate_weekly_summary`（周统计/状态变迁/下周预告）；lib.rs 定时调度：每日 08:00 早报、周日 21:00 周报；HomeView 早报横幅接 `get_today_brief` + 重新生成按钮 + 本地降级 | cargo+build |
| P1-3 Saved Filters | schema v10 新增 `saved_filters` 表；`list/save/delete_filter` 三命令；CaseFilterBar 从 localStorage 切换到 store+后端，TasksView 加已存筛选下拉 | cargo+build |
| P2-1 知识图谱 | 后端 `get_knowledge_graph`（knowledge_relations + linked_case_id + tasks.knowledge_id 真实边）；KnowledgeGraphView 重写为纯 SVG 手写力导向（删除 d3 依赖和随机连边），可拖拽/点击跳转；KnowledgeView 加入口 | cargo+build |
| P2-2 行为学习闭环 | `toggle_task`/`update_task` 支持 `actual_minutes`；due_date 推迟自动写 `deferred` 事件；`apply_estimation_calibration` 按任务模式+上下文分组校准未完成任务的 `estimated_minutes` 写回；前端 TasksView 完成时可填实际耗时，AICompanionView 学习洞察卡片 + 一键校准 | cargo+build |
| P2-3 数据蒸馏调度 | 候选记忆落 `memory_entries`（L2/pending，按内容判重）；L1 90 天清理；lib.rs 周日 23:00 蒸馏调度；确认区命令 `list/confirm/dismiss_memory`（采纳可沉淀 knowledge_items）；AICompanionView 记忆确认区 | cargo+build |
| P2-4 语音速记 | InboxView 捕获条接 `useVoiceNote`：录音按钮 + 红点脉冲 + 时长显示 + 权限降级 | build |
| B1 MCP Server | 新增 `mcp/server.rs`：127.0.0.1:37877 手写最小 HTTP JSON-RPC（initialize/tools/list/tools/call/ping）；写操作 MCP 通道拒绝（-32001，需应用内确认）；lib.rs 启动 + settings 开关 | 5/5 单测通过 |
| B2 SMTP/ICS | `send_raw_email` 真实实现（465 隐式 TLS / 587 STARTTLS，AUTH PLAIN，MIME+ics 附件，30s 超时）；`send_ics_invitation_cmd`；密码 keychain 优先；设置页 SMTP 配置 + 发送测试邀请 | cargo |
| B3 凭据 keychain | IMAP 密码保存写 keychain（service=casy-imap），连接时 keychain 优先 base64 回退；启动自动迁移（best-effort）；删除账号同步清 keychain | cargo |

**三、顺手修复的预存 bug**
- HomeView.vue：`ref`/`tauriCallSafe` 未导入（运行即崩）
- AICompanionView.vue：悬空 ConfirmDialog 引用（运行即崩）
- reports.rs / learning.rs：查询了不存在的 `tasks.completed_at`、`hearings.title/time` 列（会静默失败）
- schema v10：`memory_entries` status CHECK 扩为 `active/stale/archived/pending/merged/dismissed`（原约束挡住 pending）

**遗留（设计哲学路线图后置项，非本轮范围）**
- L3 递归确认（LLM 二次核对）、CalDAV 日历同步（M1）、动态字段系统、律师画像引导、MCP 写操作确认通道
- 语音转写（当前保存音频 + 占位文案，未接 STT）
- AICompanionView 的 `components/ConfirmDialog.vue` 已无人引用（未删）

### 2026-08-19 — 第二轮收口（设计哲学 P2/P3 剩余项）

**后端（agent-6/7，cargo check 0 error / cargo test 72 通过）**
- 提醒补偿扫描：严格相等 `days_diff == trigger_days` → 区间触发 `0 <= days_diff <= trigger_days`（deadline_before/on/after、hearing_before），离线错过的 T-N 天预警重启可补发；同日去重不变，R1-R4 分级不受影响（3 个新单测）
- 决策复核：`record_decision` 支持 `review_due`；`get_pending_decision_reviews` / `mark_decision_reviewed(id, stillValid, note)`（无效→voided）；lib.rs 每日 08:30 调度，emit `decision:review-due`
- 报表 LLM 叙事层：`AiBackend::chat_completion`；规则版先落库（narrative_source='rule'），AI 叙事覆盖（narrative_source='ai'，smart_summaries 新列）；失败静默回退规则版；过 ai_runs 审计
- L3 递归确认：`ai/recursive_check.rs`——有界上下文（深度 1-2 层，上限 50 条）+ AI 独立提示词核对 + 规则校验兜底；结果写 `decisions.recursive_checked`，缺口写 `task_events(recursion_gap)`；命令 `run_recursive_check`
- 知识块级化：schema v11——`knowledge_items` + `parent_id`/`block_type`；`task_events` CHECK 扩展 `recursion_gap`；`list_knowledge_blocks` / `get_knowledge_with_blocks`（BFS 块树，防环）
- CalDAV 日历同步（M1 核心）：`sync/caldav.rs`——test_connection/upsert_event/delete_event/get_event_etag；dispatch_reminder 新增 calendar 通道（executor='calendar'，ICS UID=job id 幂等，超时先对账不盲重 PUT，成功不再本地发，失败回退本地通知）；默认脱敏标题；命令 `test_caldav_connection` / `sync_reminders_to_calendar` / `get_calendar_sync_status`

**前端（agent-8，npm build exit 0）**
- DecisionsView：待复核区块（仍有效/已失效/递归核对）；App 层 `DecisionReviewNotice` 横幅监听 `decision:review-due`
- SmtpMcpSettings：CalDAV 卡片（配置/测试连接/同步状态/立即补同步/脱敏开关）
- KnowledgeView：块级条目顶层过滤 + 详情子块树（缩进）+ 添加子块

### 2026-08-19 — 第三轮（双向开放收口 + 差异化评估 + 律师画像）

**后端（agent-9，86 测试通过）**
- MCP 写操作确认通道：schema v12 新增 `mcp_pending_writes`；MCP 写工具不再拒绝，改返回 `{status:'pending_confirmation', write_id}` 进队列；`list/approve/reject_mcp_write` 命令，批准后走真实写并回写结果；全程 audit_events（actor='mcp'）
- CalDAV 事件清理：`toggle_task` 完成 / `delete_task` 时关联 reminder_jobs 置 cancelled + 异步 DELETE 日历事件；`cancel_reminder_jobs_for(entityType, entityId)` 显式命令
- 案件类型差异化评估：`get_case_type_metrics` / `get_all_case_type_metrics`——计算型看期限内按时完成率、探索型看阶段推进+解锁进度、成长型看活跃天数+连续无活动、通用兜底，纯 SQL
- 律师画像：`get/save_lawyer_profile`（settings 表 JSON：姓名/执业领域/案件类型/工作时段/提醒通道/onboarding_completed）

**前端（agent-10，build exit 0）**
- 设置页「外部写操作待确认」卡片（批准执行/拒绝/参数摘要/刷新）
- CaseDetailView 项目总览新增类型指标卡（按案件类型显示对应指标）
- OnboardingWizard 三步引导（姓名/领域/案件类型/工作时段/提醒通道）+ App 启动检查 + 设置页画像 Tab + 首页早报问候语带姓名

### 2026-08-19 — 第四轮（STT/隐性关联/报表历史）

**后端（agent-11，89 测试通过）**
- 语音转写：`transcribe_voice_note`——OpenAI 兼容 `/audio/transcriptions`（手工 multipart，stt_model 可配），未配置返回友好降级文案；过 ai_runs 审计
- 隐性关联学习（设计哲学 §3.2 通道 B）：`ai/insights.rs`——有界数据摘要（活跃案件≤30/决策≤20/事件统计/waiting 超期）→ AI 产出关联洞察（≤5 条，带置信度+source_ref）→ `ai_insights`（pending，判重）；每周六 22:00 调度；确认区四命令（confirm 可沉淀 knowledge_items）
- 报表历史：`list_summaries(summaryType?, limit?)` 含 narrative_source

**前端（agent-12，build exit 0）**
- InboxView：音频条目「转写」按钮（loading/成功刷新/失败显示后端文案）
- AICompanionView：「关联洞察」卡片（立即分析/采纳入库/确认/忽略/置信度/来源）+「报表历史」卡片（日报/周报切换、AI/规则来源标记、Markdown 展开）

---

## 最终状态（2026-08-19 收口）

- `cargo check`：0 error
- `cargo test --lib`：89/89 通过
- `npm run build`：exit 0
- schema：v12（saved_filters / mcp_pending_writes / knowledge 块级 / narrative_source / recursion_gap）

**仍为路线图后置项（不冒充已实现）**
- 动态字段系统（P4，需 cases 表重构为字段定义+单元格）
- 提醒时机智能（画像工作时段已存，学习→提醒调度的联动未接）
- 批处理事件推送（当前轮询，目标事件流）
- 记忆生命周期全自动（陈旧归档/近似合并有基础逻辑，未做全量自动调度验证）
